services:
  db:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: bolanobrasil
      POSTGRES_USER: bolanobrasil
      POSTGRES_PASSWORD: troque_essa_senha
    volumes:
      - pgdata:/var/lib/postgresql/data
    ports:
      - "5432:5432"
    restart: always

volumes:
  pgdata:
